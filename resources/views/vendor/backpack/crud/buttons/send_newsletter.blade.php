{{-- <a href="{{ route('admin.newsletter.form') }}" class="btn btn-primary">
    <i class="la la-envelope"></i> Send Newsletter
</a> --}}
<a href="{{ route('admin.newsletter.form') }}"
   class="btn btn-success"
   title="Send Newsletter to Subscribers">
   <i class="la la-paper-plane"></i> Send Newsletter
</a>
